# --- Backend Configuration ---
# Required for running the backend locally for development.
# Note: PyPI users do NOT need this file!

GROQ_API_KEY=gsk_...
SUPABASE_URL=https://...supabase.co
SUPABASE_KEY=eyJhb...

# Use the Session/Direct Connection string for Supabase (Port 5432)
# Do NOT use the Transaction Pooler (Port 6543) as it breaks SQLAlchemy schema migrations
DATABASE_URL=postgresql+psycopg2://...